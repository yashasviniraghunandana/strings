
void reverseString(char* s, int sSize){
    for(int i=0;i<sSize/2;i++)
    {
        char temp = s[i];
        printf("%c",temp);
        printf("%c",s[sSize-1-i]);
        //*(s+i) = *(s+sSize-i-1);
        s[i] = s[sSize-i-1];
        s[sSize-i-1] = temp;
    //swap(s[i],s[sSize-i+1]);
    }
}
